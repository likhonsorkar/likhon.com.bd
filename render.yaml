services:
  - type: web
    name: likhon-com-bd
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput"
    startCommand: "gunicorn likhoncombd.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
